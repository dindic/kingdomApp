import { OpticonsDirective } from './opticons.directive';

describe('OpticonsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpticonsDirective();
    expect(directive).toBeTruthy();
  });
});
